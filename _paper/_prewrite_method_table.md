# Method 写前表格(share-method-craft 第 2 段)

每个模块答四问。**填不出来的格子必须标「待补/证据缺失」,不得编造。**

| Module | Input → Output | Why Needed | Why It Works | Evidence Later |
|---|---|---|---|---|
| **M1 问题设定与经济口径** | θ ∈ R^(6×4) 分段注入乘子 → 含水成本 NPV(r) | 只算油收入时"注多少水"退化为边界解,必须靠外加固定预算兜底;给水定价才让总注入量成为**内生**决策 | 注水成本 c_inj 与采出水处理成本 c_prod 进入目标函数,使边际注水存在最优点 | Table 1(算例设定);§4.1 基准 NPV = 1773.4 M$ |
| **M2 代理模型** | θ(24 维) → 多井多相产量轨迹 (W×P×T) | 全物理模拟一次约 1 分钟,任何需要大量试错的决策方法不可行 | 算子学习(PosNet + 傅里叶位置嵌入),推理时不吃历史序列;7400 训练样本 + 500 独立确认集 | Fig. 2;确认集场级误差 **0.369%**,R²=0.9959,**18.1 µs/方案(≈3.3×10⁶ 倍)** |
| **M3 代理的失效边界** | 优化区候选 10 个 → 代理排名 vs OPM Flow 实测排名 | **决定整个框架为什么必须有裁判**。若代理排名在最优解附近仍可靠,直接用代理选解即可,框架就没必要 | 优化器把候选压进代理认为好的区域,系统性放大代理的乐观偏差(optimizer's curse) | Fig. 3;随机 θ 上 ρ=**0.998**、top-1 命中 100%;优化区 ρ=**−0.21**、top-1 命中 **0**;代理选中的正是它高估最狠的那个(高估倍数 10.6 vs 中位 1.66);top-1 regret **46.0 M$** |
| **M4 消息协议与出处** | 角色断言 → 带 provenance 的类型化消息 | 没有可引用证据的角色不该有发言权;需要让"某角色是否实质"由协议判定而非争论 | 每条消息必带 (source_id, source_region, source_type, source_sha256);校验不过即拒收 | §3.3;source_type 区分 deck/simulation/measurement/literature/reasoning |
| **M5 连通性制图** | 已模拟算例库 → 注采影响矩阵 | 角色需要客观的井间影响先验,否则只能靠文献类比 | 各生产井累计油对各注入井累计注水的偏回归;报告设计矩阵条件数 | §4.4;471 算例,条件数 **2.05**;F-1H 8.57 > F-3H > F-2H > F-4H 2.81 |
| **M6 多角色决策器** | 7 个数据切片 + 上一轮实测 → 候选方案 | 注水决策同时受连通性、地质力学、经济、约束等多域证据约束,单一视角易漏 | 各角色只读本域切片,先独立表态再互相质疑;总工按**证据强度**裁决(实测类 > 文献类),不按人头 | Fig. 4;Team +361.3 M$ (n=10) |
| **M7 程序化可行性闸门** | 候选 θ → 通过/否决 | 语言模型可以被说服,算术边界不能 | 注入预算偏离、θ 箱约束、按实测井筒注入能力做饱和检查;**不交给语言模型** | §4.3;被否决方案不计入成绩,由 test_gaia 守 |
| **M8 全物理裁判** | 候选 θ → OPM Flow 实测 field_cum | M3 证明代理在最优解附近不可信,因此最终取值只能来自模拟器 | 所有累计量读模拟器自报 FOPT/FWIT/FWPT,不用井曲线梯形积分(两口径差 0.58%) | 全文所有 NPV;fc_truth 单一口径 + 7 项测试 |
| **M9 评价协议** | 各臂 10 次重复 → 均值/中位/sd/CI | LLM 输出随机,单次消融表会给出相反结论 | 先测同配置重复性(sd 51.3 M$),再解释任何消融;臂顺序交错随机化 | Table 2;Fig. 4 |

## 待补 / 证据缺失

- **人类专家基线不存在** → 正文不得出现 "expert-level"。
- **跨油藏证据不存在** → tilt 不得称 "transferable law"。
- **方差差异不可检验**(Levene p=0.453) → 不得称多角色提升稳健性。
