# 归档:2026-09-08 夜间 LLM 断线导致的 6 次空跑

这 6 次的 loop json 里没有任何有效解,原因是**全部角色的 LLM 调用失败**
(`RuntimeError: LLM rc=1 (重试 4 次仍失败)`),额度耗尽,不是决策失败。

方法章节的口径把两种情况分开:
- **候选全被否决** → 记 0 分(作业方会执行基准方案),**不丢弃**
- **基础设施故障** → 重跑,不计入

两臂各 3 次,对称,不构成偏倚。已用同一命令重跑补齐。
