# Storyline 变更记录

## v3 — 2026-09-06(current-user-approved)

**Gaia 智能体团队。** 核心贡献从「多角色协作」改为「分层智能体架构 + 权限按实测能力分配」,
四条贡献:架构本身、代理模型、代理的能力边界(能筛不能判)、全物理验证的经济价值。
注水调度降为 case study,论文导向为方法。

相对 v2 的差异:
- 智能体从「七个推理角色」扩展为「14 个 Agent / 5 层」,覆盖数据、知识、推理、评估、裁决全链
- 新增 C3「代理能筛不能判」为独立贡献 —— 它解释了架构为什么必须这样分权
- **单 Agent 消融臂不进正文**(用户裁定,数据保留)
- 发布 `gaia_sdk` 可运行实现,并用 15 项测试守住"SDK 与已跑实验一字不差"

## v2 — 2026-09-03(已被 v3 取代)

Gaia Agent Framework;贡献是多角色智能体决策。被取代原因见 v3。

## v1 — 2026-08-29(已作废)

注入预算受控下时序调度主导短期/最终采收权衡。用户判定不构成论文贡献:
油田历史已发生,给单个油田的策略建议无法验证。
